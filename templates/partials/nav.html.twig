<nav
    class="navbar navbar-expand-lg px-lg-5 border-1 border-bottom border-dark-subtle"
>
    <div class="container-fluid">
        <button
                class="navbar-toggler px-3 text-white d-md-none"
                type="button"
                data-bs-toggle="offcanvas"
                data-bs-target="#sidebarMenu"
                aria-controls="sidebarMenu"
                aria-expanded="false"
                aria-label="Toggle navigation"
        >
            <span class="navbar-toggler-icon"></span>
        </button>
        <a class="navbar-brand" href="/">
            <i class="fa-solid fa-screwdriver-wrench fs-2"></i>
            <i class="fa-solid fa-car fs-2"></i>
        </a>
        <div class="collapse navbar-collapse text-white" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            </ul>

            <div class="d-flex">
                {% set user = session.user %}
                {% if user %}
                <ul class="navbar-nav dropdown">
                    <li class="nav-item">
                        <a class="nav-link active dropdown-toggle"
                           href="#"
                           role="button"
                           data-bs-toggle="dropdown"
                           aria-expanded="false"
                        >
                            {{ user.username }}
                        </a>

                        <ul class="dropdown-menu dropdown-menu-end">
                            <li><a class="dropdown-item" href="/profile">Profil</a></li>
                            <li>
                                <hr class="dropdown-divider">
                            </li>
                            <li>
                                <form action="/logout" method="POST">
                                    <input type="hidden" name="_method" value="DELETE">
                                    <button class="dropdown-item">Wyloguj</button>
                                </form>
                            </li>
                        </ul>
                    </li>
                </ul>
                {% else %}
                    <a class="btn text-body"
                       href="/login"
                    >
                        Zaloguj się
                    </a>
                {% endif %}
            </div>
        </div>
    </div>
</nav>