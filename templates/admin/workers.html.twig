{% extends "/base.html.twig" %}

{% block title %}
    Pracownicy
{% endblock title %}

{% block main %}
    <div
        class="col-md-9 ms-sm-auto col-lg-10 px-md-4 d-flex flex-column gap-2 my-5"
        style="min-height: calc(100vh - 56px);">
        <h1 class="text-center">Lista pracowników</h1>

        <table class="table table-hover">
            <thead>
            <tr>
                <th>Lp</th>
                <th>Imię</th>
                <th>Nazwisko</th>
                <th>Stanowisko</th>
            </tr>
            </thead>
            <tbody>
            {% set i = 1 %}
            {% for worker in workers %}
                <tr onclick="window.location='/update-worker?id={{ worker.Id }}'" style="cursor: pointer;">
                    <td>
                        {{ i }}
                    </td>
                    <td>
                        {{ worker.Firstname }}
                    </td>
                    <td>
                        {{ worker.Lastname }}
                    </td>
                    <td>
                        {{ worker.Role }}
                    </td>
                </tr>
                {% set i = i +1 %}
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock main %}
